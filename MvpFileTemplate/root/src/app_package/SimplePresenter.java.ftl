package ${packageName}.presenter;

import ${packageName}.presenter.view.${viewClass};

public class ${presenterClass} extends BasePresenter<${viewClass}> {
    public ${presenterClass}(${viewClass} mvpView) {
        super(mvpView);
    }
}
